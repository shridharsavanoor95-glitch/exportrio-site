// Exportrio – Premium Multi-Page Website
// Specialized in Indian Rice Exports

// =========================
// app/layout.tsx
// =========================
import "./globals.css";
import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

export const metadata = {
  title: "Exportrio | Premium Indian Rice Exporter",
  description: "Exportrio exports premium Indian rice worldwide, delivering quality, compliance, and reliability.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}

// =========================
// components/Navbar.tsx
// =========================
import Link from "next/link";
import { Button } from "@/components/ui/button";

export function Navbar() {
  return (
    <header className="border-b border-neutral-800 bg-neutral-950">
      <div className="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
        <Link href="/" className="text-xl font-semibold">Exportrio</Link>
        <nav className="hidden md:flex gap-8 text-sm text-neutral-300">
          <Link href="/products">Products</Link>
          <Link href="/markets">Markets</Link>
          <Link href="/quality">Quality</Link>
          <Link href="/contact">Contact</Link>
        </nav>
        <Button size="sm">Request Quote</Button>
      </div>
    </header>
  );
}

// =========================
// components/Footer.tsx
// =========================
export function Footer() {
  return (
    <footer className="border-t border-neutral-800 bg-neutral-950">
      <div className="max-w-7xl mx-auto px-6 py-10 flex flex-col md:flex-row justify-between gap-4 text-sm text-neutral-400">
        <div>
          <div className="font-semibold text-neutral-200">Exportrio</div>
          <p className="mt-2">Premium Indian Rice Exporters.</p>
        </div>
        <div>© {new Date().getFullYear()} Exportrio. All rights reserved.</div>
      </div>
    </footer>
  );
}

// =========================
// app/page.tsx (Home)
// =========================
import { Navbar } from "@/components/Navbar";
import { Footer } from "@/components/Footer";
import { Button } from "@/components/ui/button";

export default function HomePage() {
  return (
    <div className="min-h-screen bg-neutral-950 text-neutral-100">
      <Navbar />
      <section className="max-w-7xl mx-auto px-6 py-28">
        <h1 className="text-6xl font-bold">
          Premium Indian Rice,<br />FOB India Exports Worldwide
        </h1>
        <p className="mt-6 max-w-2xl text-neutral-300">
          Exportrio is a trusted exporter of premium Indian rice, supplying
          global buyers on a <strong>FOB India</strong> basis with consistent quality,
          transparent pricing, and reliable documentation.
        </p>
        <div className="mt-10 flex gap-4">
          <Button size="lg">Request FOB Quote</Button>
          <Button size="lg" variant="outline">View Products</Button></Button>
        </div>
      </section>
      <Footer />
    </div>
  );
}

// =========================
// app/products/page.tsx
// =========================
import { Navbar } from "@/components/Navbar";
import { Footer } from "@/components/Footer";

export default function ProductsPage() {
  return (
    <div className="bg-neutral-950 text-neutral-100 min-h-screen">
      <Navbar />
      <section className="max-w-7xl mx-auto px-6 py-24">
        <h1 className="text-5xl font-bold">Our Rice Products</h1>
        <p className="mt-4 text-neutral-300">
          All products are supplied on a <strong>FOB India</strong> basis with a
          minimum order quantity of <strong>25 MT / 20ft container</strong>.
        </p>
        <div className="mt-10 space-y-12 text-neutral-300">
          <div>
            <h2 className="text-2xl font-semibold">Basmati Rice Specifications</h2>
            <table className="mt-4 w-full border border-neutral-800 text-sm">
              <tbody>
                <tr><td className="p-3 border">Varieties</td><td className="p-3 border">1121, 1509, Traditional</td></tr>
                <tr><td className="p-3 border">Grain Length</td><td className="p-3 border">8.2 – 8.4 mm</td></tr>
                <tr><td className="p-3 border">Broken</td><td className="p-3 border">≤ 2%</td></tr>
                <tr><td className="p-3 border">Moisture</td><td className="p-3 border">≤ 14%</td></tr>
                <tr><td className="p-3 border">Packaging</td><td className="p-3 border">5kg–50kg, PP / BOPP</td></tr>
                <tr><td className="p-3 border">MOQ</td><td className="p-3 border">25 MT / 20ft (FOB India)</td></tr>
              </tbody>
            </table>
          </div>

          <div>
            <h2 className="text-2xl font-semibold">Non-Basmati & Parboiled Rice Specifications</h2>
            <table className="mt-4 w-full border border-neutral-800 text-sm">
              <tbody>
                <tr><td className="p-3 border">Varieties</td><td className="p-3 border">IR64, Sona Masoori, Swarna</td></tr>
                <tr><td className="p-3 border">Grain Length</td><td className="p-3 border">5.5 – 6.5 mm</td></tr>
                <tr><td className="p-3 border">Broken</td><td className="p-3 border">≤ 5%</td></tr>
                <tr><td className="p-3 border">Moisture</td><td className="p-3 border">≤ 14%</td></tr>
                <tr><td className="p-3 border">Packaging</td><td className="p-3 border">25kg / 50kg bags</td></tr>
                <tr><td className="p-3 border">MOQ</td><td className="p-3 border">25 MT / 20ft (FOB India)</td></tr>
              </tbody>
            </table>
          </div>
        </div>
      </section>
      <Footer />
    </div>
  );
}

// =========================
// app/quality/page.tsx
// =========================
import { Navbar } from "@/components/Navbar";
import { Footer } from "@/components/Footer";

export default function QualityPage() {
  return (
    <div className="bg-neutral-950 text-neutral-100 min-h-screen">
      <Navbar />
      <section className="max-w-7xl mx-auto px-6 py-24">
        <h1 className="text-5xl font-bold">Quality, Compliance & Certifications</h1>
        <a href="/Exportrio_Company_Profile.pdf" className="inline-block mt-6 px-6 py-3 rounded-lg bg-indigo-600 text-white font-medium hover:bg-indigo-500">
          Download Company Profile (PDF)
        </a>
        <p className="mt-6 max-w-3xl text-neutral-300">
          Exportrio follows strict quality control procedures to ensure every
          shipment meets international food safety and export compliance standards.
          All rice is supplied on a <strong>FOB India</strong> basis with complete
          documentation.
        </p>

        <div className="mt-12 grid md:grid-cols-3 gap-8">
          <div className="border border-neutral-800 rounded-xl p-6 bg-neutral-900">
            <h3 className="text-xl font-semibold">APEDA Registered</h3>
            <p className="mt-3 text-sm text-neutral-300">
              Registered with the Agricultural and Processed Food Products Export
              Development Authority (APEDA), Government of India, authorizing
              Exportrio to export rice globally.
            </p>
          </div>

          <div className="border border-neutral-800 rounded-xl p-6 bg-neutral-900">
            <h3 className="text-xl font-semibold">FSSAI Compliant</h3>
            <p className="mt-3 text-sm text-neutral-300">
              All products comply with Food Safety and Standards Authority of India
              (FSSAI) regulations, ensuring hygiene, traceability, and food safety.
            </p>
          </div>

          <div className="border border-neutral-800 rounded-xl p-6 bg-neutral-900">
            <h3 className="text-xl font-semibold">ISO / HACCP Mills</h3>
            <p className="mt-3 text-sm text-neutral-300">
              Rice is sourced from ISO and HACCP certified milling facilities with
              strict quality control, grading, and packaging standards.
            </p>
          </div>
        </div>

        <div className="mt-12 max-w-3xl text-neutral-300">
          <p>
            Additional services such as <strong>fumigation, phytosanitary
            certificates, and third-party inspection (SGS / Intertek)</strong> are
            provided as per buyer requirements.
          </p>
        </div>
      </section>
      <Footer />
    </div>
  );
}

      <Footer />
    </div>
  );
}

// =========================
// app/markets/page.tsx
// =========================
import { Navbar } from "@/components/Navbar";
import { Footer } from "@/components/Footer";

export default function MarketsPage() {
  return (
    <div className="bg-neutral-950 text-neutral-100 min-h-screen">
      <Navbar />
      <section className="max-w-7xl mx-auto px-6 py-24">
        <h1 className="text-5xl font-bold">Indian Rice Exporter to Africa</h1>
        <p className="mt-6 max-w-4xl text-neutral-300">
          Exportrio is a trusted <strong>Indian rice exporter to Africa</strong>, supplying
          bulk FOB shipments to Benin, Senegal, and Kenya. Our rice meets regional
          consumption preferences, competitive pricing expectations, and strict
          export documentation requirements.
        </p>
        <p className="mt-6 max-w-3xl text-neutral-300">
          Exportrio supplies premium Indian rice to key global markets on a <strong>FOB India</strong> basis with shipments from <strong>Nhava Sheva, Mundra, and Kandla ports</strong> and a standard MOQ of <strong>25 MT / 20ft container</strong>.
          tailored specifications, packaging, and logistics.
        </p>

        <div className="mt-12 grid md:grid-cols-2 gap-8">
          <div>
            <h3 className="text-2xl font-semibold">Africa</h3>
            <ul className="mt-4 text-neutral-300 space-y-2">
              <li>• Benin – Long grain & parboiled rice</li>
              <li>• Senegal – Parboiled & non-basmati varieties</li>
              <li>• Kenya – IR64, Sona Masoori, white rice</li>
            </ul>
          </div>

          <div>
            <h3 className="text-2xl font-semibold">Middle East</h3>
            <p className="mt-4 text-neutral-300">
              Premium Basmati (1121 & 1509) for wholesale and foodservice buyers.
            </p>
          </div>

          <div>
            <h3 className="text-2xl font-semibold">Asia</h3>
            <p className="mt-4 text-neutral-300">
              Competitive non-basmati and parboiled rice for regional distributors.
            </p>
          </div>

          <div>
            <h3 className="text-2xl font-semibold">Europe</h3>
            <p className="mt-4 text-neutral-300">
              High-quality, compliant basmati rice meeting EU food standards.
            </p>
          </div>
        </div>
      </section>
      <Footer />
    </div>
  );
}

// =========================
// app/contact/page.tsx
// =========================
import { Navbar } from "@/components/Navbar";
import { Footer } from "@/components/Footer";
import { Button } from "@/components/ui/button";

export default function ContactPage() {
  return (
    <div className="bg-neutral-950 text-neutral-100 min-h-screen">
      <Navbar />
      <section className="max-w-3xl mx-auto px-6 py-24">
        <h1 className="text-5xl font-bold">Request an FOB India Quote</h1>
        <p className="mt-4 text-neutral-300">
          MOQ: <strong>25 MT / 20ft container</strong><br />
          Available FOB ports: <strong>Nhava Sheva, Mundra, Kandla</strong>
        </p>
        <form className="mt-10 space-y-4">
          <input className="w-full p-3 rounded bg-neutral-900 border border-neutral-800" placeholder="Company Name" />
          <input className="w-full p-3 rounded bg-neutral-900 border border-neutral-800" placeholder="Email" />
          <input className="w-full p-3 rounded bg-neutral-900 border border-neutral-800" placeholder="Destination Country" />
          <textarea className="w-full p-3 rounded bg-neutral-900 border border-neutral-800" placeholder="Rice Type, Quantity & Preferred FOB Port" rows={4} />
          <Button size="lg">Submit Inquiry</Button>
        </form>
      </section>
      <Footer />
    </div>
  );
}
